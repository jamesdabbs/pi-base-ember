import {
  renderMarkdown
} from 'pi-base/helpers/render-markdown';

module('RenderMarkdownHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = renderMarkdown(42);
  ok(result);
});
