import {
  previewDescription
} from 'pi-base/helpers/preview-description';

module('PreviewDescriptionHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = previewDescription(42);
  ok(result);
});
